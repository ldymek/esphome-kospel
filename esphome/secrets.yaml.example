# Copy to secrets.yaml next to the generated YAML. NEVER commit the real file.
wifi_ssid: "your-iot-ssid"
wifi_password: "your-wifi-password"
api_key: "32-byte-base64-key"          # generate: openssl rand -base64 32
ota_password: "long-random-password"
fallback_ap_password: "long-random-password"
